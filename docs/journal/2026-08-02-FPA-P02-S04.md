# Session 2026-08-02 — FPA-P02-S04

## Session objective

Complete ADR-0004's Phase 2 account-security hardening without introducing
Phase 3 family roles or mandatory MFA.

## Work completed

- Added reusable database-session and remember-token revocation for password
  reset, password change and user-requested global logout.
- Added an operator account-revocation command and blocked revoked accounts from
  future login through the generic invalid-credentials path.
- Applied one 15-to-255-character password policy with padded, short-timeout,
  fail-open HIBP screening that does not log password-derived material.
- Added named password-reset throttling without hard lockout or CAPTCHA.
- Enabled optional confirmed Fortify TOTP, recovery codes and MFA security-event
  auditing.
- Added typed account-security and MFA API modules, TanStack Query
  query/mutation hooks, accessible security forms and a two-factor challenge.
- Adopted strict TypeScript, the `@/` alias, query cancellation, React Hook Form,
  Zod, MSW, JSX accessibility linting and a root error boundary in the concrete
  flows that require them.

## Key implementation notes

Laravel's stock compromised-password verifier already sends the HIBP
`Add-Padding` header and fails open, but defaults to a 30-second timeout and
reports the caught exception, whose request URL contains the password-derived
SHA-1 prefix. The application replaces only that verifier contract with a
1.5-second implementation that records a provider/outcome warning without the
exception or request URL.

The emailed invitation-token exchange consumes server state, so it is modeled
as a guarded TanStack mutation. Its mutation-cache state survives React Strict
Mode's effect replay, preserving the exactly-once exchange already enforced by
the frontend test.

## Verification

- Foundation, documentation, Compose configuration, formatting, linting,
  typechecking, contracts, JSON and diff checks passed.
- Laravel: 42 tests passed, 275 assertions, including distinct compromised-
  password timeout, remote-failure, rejection and clean-logging cases.
- React: 19 tests passed across 8 files.
- Image AI: 4 tests passed.
- PHPStan, strict TypeScript, ESLint with JSX accessibility rules and the
  production web build passed.
- NPM and Composer reported no known dependency vulnerabilities.
- The live PostgreSQL migration ran successfully; infrastructure and
  observability smoke checks passed.
- Browser verification confirmed the account-security landmark, heading and
  label structure, password autocomplete semantics, viewport fit and an empty
  warning/error console without submitting a security action.

## Delayed independent review corrections

- Rechecked account revocation immediately before Fortify completes an
  in-flight MFA challenge, rejecting revoked users generically and invalidating
  the pending challenge session.
- Switched new password hashes to Argon2id without reducing the accepted
  15-to-255-character policy; the rebuilt PHP container supports Argon2id, and
  regression coverage proves legacy bcrypt verification and login rehashing.
- Disabled repeat viewing of plaintext recovery codes. Codes are returned only
  by a password-confirmed generation or regeneration response.
- Required an explicit console-operator reference and retained it in account-
  revocation audit metadata.
- Removed the obvious account-state timing distinction by performing one hash
  verification for unknown, revoked and active wrong-password login attempts.
- Corrected secure-cookie example configuration, reset-submission throttling,
  MFA limiter fallback, named account-security limiters, verifier registration,
  the one-time invitation-exchange standards reference and frontend security-
  flow coverage within the touched Phase 2 scope.

## Review state

The implementation was committed locally as `d098646` before the delayed
independent review had completed. Phase 2 remained under review, the
premature local `phase-2-s04` and `phase-2` completion tags were removed, and
no completion approval had yet been granted. The review's accepted findings
were then corrected in the working tree, and a focused independent re-review
verified each correction directly: by reading the corrected code, by rerunning
the full test suite and repository verification gate, and by a live
reproduction of the original revoked-mid-challenge exploit against the
rebuilt local stack, confirming the recovery-code-based challenge attempt is
now rejected and no session is established. That re-review found no remaining
blocking or important issues and approved completion.

## Completion

FPA-P02-S04 and the Phase 2 acceptance gate are complete. The completion
commit receives both the `phase-2-s04` and `phase-2` tags.

## AI-assisted development record

Codex implemented and tested the account-security hardening. David remains
responsible for reviewing the diff and approving repository history.
